# Yahoo API secrets — never commit
credentials.json
token.json
*.pem
.env

# Python
__pycache__/
*.pyc

# Per-draft state, created wherever you run the draft
draft_state.json

# Arena: commit curated reports only, not raw tournament dumps
arena/results/*.json
